<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RUGGPT - AI That Exposes Rugpulls</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            background-color: #111;
            color: #fff;
        }
        .container {
            max-width: 800px;
            margin: 50px auto;
            padding: 20px;
            background-color: #222;
            border-radius: 10px;
        }
        button {
            background-color: #ff4444;
            color: white;
            border: none;
            padding: 15px 20px;
            font-size: 18px;
            cursor: pointer;
            border-radius: 5px;
        }
        button:hover {
            background-color: #cc0000;
        }
        input {
            width: 80%;
            padding: 10px;
            margin: 10px 0;
            font-size: 16px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>🚨 RUGGPT - The AI That Exposes Rugpulls 🚨</h1>
        <p>Enter a contract address below to check for rug risk:</p>
        <input type="text" id="contractAddress" placeholder="Paste contract address here">
        <button onclick="checkRugRisk()">Analyze Contract</button>
        <p id="result"></p>
        <br>
        <a href="https://app.uniswap.org/" target="_blank">
            <button>Buy $RUGGPT on Uniswap</button>
        </a>
    </div>

    <script>
        function checkRugRisk() {
            let address = document.getElementById("contractAddress").value;
            if (!address) {
                document.getElementById("result").innerHTML = "⚠️ Please enter a contract address.";
                return;
            }
            document.getElementById("result").innerHTML = "🔍 Scanning...";
            
            setTimeout(() => {
                let risk = Math.floor(Math.random() * 100) + 1; // Mock AI response
                document.getElementById("result").innerHTML = `🚨 Rug Risk: ${risk}% 🚨`;
            }, 2000);
        }
    </script>
</body>
</html>
